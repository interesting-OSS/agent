"""墨庐 LangGraph 模块 — 基于 LangGraph 的多 Agent 章节生成流水线。

用法:
    from app.langgraph.generation_graph import run_chapter_generation
    result = await run_chapter_generation(novel_id, chapter_number, ...)
"""
