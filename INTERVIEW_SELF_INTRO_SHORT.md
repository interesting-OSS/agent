我独立开发了一个多智能体小说写作系统，用 LangGraph StateGraph 编排 4 个 Agent（Writer/Architect/Inspector/Guardian）协作生成章节。流程上先由 Architect 与 Guardian 并行做情节检查与类型预审，再由 Writer 生成正文，生成后 Guardian 扫描禁止术语、Inspector 做 10 维质量评分（类型合规/AI味/情节逻辑/角色一致性等），Guardian 发现 fatal 或 Inspector 判定不通过则回到 Writer 重写，通过才结束。Agent 间通过 State 中的 files 字段传递结构化中间结果。工程上封装了 LLM Provider 抽象层，统一 DeepSeek/Kimi/Qwen 三家 OpenAI 兼容 API，自建 LangChain ChatModel 适配器让自研 provider 接入 LangGraph。每次生成前 ContextAssembler 从数据库拼装 4 层上下文（类型约束/近期事件/历史摘要/大纲规划）注入 Writer，超预算时按优先级裁剪。后端 FastAPI + SQLAlchemy + SQLite，前端 Next.js + React + Ant Design，章节生成通过 SSE 流式推送到前端。全栈独立开发。
